# AroundYou DevBuild

For local testing use the script defined inside of `package.json`
```shell
> npm start
```
Add the `.env` file in the project root with the database uri

- Default: `database = 'mongodb://127.0.0.1:27017'`